using UnityEngine;
using System.Collections;

public class WayTracerFreeMovement : WayTracer {

    /*
    void MoveTowardNewLocation()
    {
        float step = speedOfMovement * Time.deltaTime;

        if (TravellingToMapNode == null)
        {
            TravellingToMapNode = ParentEmitter.GetRandomRoadNode();
        }
        transform.position = Vector3.MoveTowards(transform.position, TravellingToMapNode.LocationInUnits, step);
    }
    */

}
